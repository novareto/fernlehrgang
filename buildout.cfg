[buildout]
extends =
    profiles/development.cfg

parts += 
    app

[app]
eggs += 
    fernlehrgang[odbc] 
    pdbpp
    gunicorn
    superlance
extra-paths = ${buildout:parts-directory}/mxodbc

[settings]
SCHEMA = FLGUTF8
